{
  "name": "blog-static-site",
  "version": "1.0.0",
  "description": "Static blog that pre-renders markdown posts into HTML assets",
  "scripts": {
    "build": "node scripts/build.js"
  },
  "dependencies": {
    "fs-extra": "^11.2.0",
    "gray-matter": "^4.0.3",
    "marked": "^12.0.2"
  }
}

